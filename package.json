{
  "name": "ShopFull",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^7",
    "@sap/xssec": "^3.6.1",
    "express": "^4",
    "html2pdf.js": "^0.9.0"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^1",
    "@sap/ux-specification": "^1.120.6"
  },
  "scripts": {
    "start": "cds-serve",
    "deploy:sqlite": "cds deploy --to sqlite:shop.sqlite",
    "watch-shop": "cds watch --open shop/webapp/index.html?sap-ui-xx-viewCache=false",
    "debloy:hana": "cds deploy --to hana:Shop"
  }
}
